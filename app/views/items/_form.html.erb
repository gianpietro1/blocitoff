<tr>
     <%= form_for [list, item] do |f| %>
       <td class="form-group" colspan="3">
         <%= f.text_field :name, class: 'form-control', placeholder: "Add new item" %>
       </td>
       <td class="form-group vertical-center center">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </td>
     <% end %>
</tr>